name: Build Shade Editor for Windows

permissions:
  contents: write

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: shade-editor-windows
      - name: Check
        run: cargo check --target x86_64-pc-windows-msvc
      - name: Test
        run: cargo test --target x86_64-pc-windows-msvc
      - name: Build release
        run: cargo build --release --target x86_64-pc-windows-msvc
      - name: Prepare artifact
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force artifact | Out-Null
          Copy-Item target/x86_64-pc-windows-msvc/release/ShadeEditor.exe artifact/ShadeEditor.exe
          Copy-Item README.md artifact/README.md
          Copy-Item LICENSE artifact/LICENSE
      - uses: actions/upload-artifact@v4
        with:
          name: ShadeEditor-win-x64
          path: artifact
      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ github.ref_name }}" `
            target/x86_64-pc-windows-msvc/release/ShadeEditor.exe `
            --title "Shade Editor ${{ github.ref_name }}" `
            --notes-file RELEASE_NOTES.md `
            --latest
