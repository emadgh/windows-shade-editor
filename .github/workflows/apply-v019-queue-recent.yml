name: Apply v0.19 Export Queue and Recent Projects

on:
  push:
    branches:
      - feature/v019-queue-recent

permissions:
  contents: write

jobs:
  apply-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Apply implementation
        run: python .github/scripts/apply_v019_queue_recent.py
      - name: Fix metrics separator quoting
        run: python .github/scripts/fix_v019_queue_metrics_quote.py
      - name: Format
        run: cargo fmt --all
      - name: Format check
        run: cargo fmt --all -- --check
      - name: Check
        run: cargo check --release --locked
      - name: Test
        run: cargo test --release --locked
      - name: Release build
        run: cargo build --release --locked
      - name: Commit validated implementation
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(v0.19): improve Export Queue and recent projects (#39)"
          git push
      - name: Upload validated executable
        uses: actions/upload-artifact@v4
        with:
          name: ShadeEditor-v019-queue-recent-windows-x64
          path: target/release/ShadeEditor.exe
          if-no-files-found: error
