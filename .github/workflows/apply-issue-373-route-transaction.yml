name: Apply Issue 373 Route Transaction

on:
  push:
    branches:
      - codex/issue-373-conversion-routes

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[issue-373-route-transaction]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: codex/issue-373-conversion-routes
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.x'
      - name: Apply route transaction core
        run: python scripts/issue373_route_transaction.py
      - name: Verify diff
        run: git diff --check
      - name: Remove temporary patch scaffolding
        run: git rm .github/workflows/apply-issue-373-route-transaction.yml scripts/issue373_route_transaction.py
      - name: Commit transaction core
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Add route-owned conversion transaction [issue-373-route-transaction]"
          git push origin HEAD:codex/issue-373-conversion-routes
