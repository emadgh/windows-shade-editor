name: Apply Issue 372 Test Import Fix

on:
  push:
    branches:
      - codex/issue-372-unified-color-conversion

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[issue-372-test-import]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: codex/issue-372-unified-color-conversion
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.x'
      - name: Patch candidate test import
        run: python scripts/issue372_test_import_patch.py
      - name: Verify diff
        run: git diff --check
      - name: Remove temporary patch scaffolding
        run: git rm .github/workflows/codex-issue-372-test-import.yml scripts/issue372_test_import_patch.py
      - name: Commit fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix candidate preview test import [issue-372-test-import]"
          git push origin HEAD:codex/issue-372-unified-color-conversion
