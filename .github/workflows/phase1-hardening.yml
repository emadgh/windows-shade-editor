name: Validate Phase 1 Export Hardening

permissions:
  contents: write

on:
  push:
    branches:
      - hardening/v0.18

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc
          components: rustfmt
      - name: Apply Phase 1 migration
        run: python tools/apply_phase1_hardening.py
      - name: Format
        run: cargo fmt --all
      - name: Check Windows target
        run: cargo check --target x86_64-pc-windows-msvc
      - name: Test Windows target
        run: cargo test --target x86_64-pc-windows-msvc
      - name: Remove temporary Phase 1 infrastructure
        shell: pwsh
        run: |
          git rm tools/apply_phase1_hardening.py
          git rm .github/workflows/phase1-hardening.yml
      - name: Commit validated Phase 1 source
        shell: pwsh
        run: |
          git config user.name "Shade Editor CI"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git status --short
          git commit -m "[skip ci] Phase 1: harden export queue semantics and transport"
          git push origin HEAD:hardening/v0.18
